# Core
fastapi==0.115.12
uvicorn==0.34.3
gunicorn==23.0.0

# Database
sqlalchemy==2.0.41
alembic==1.16.2
psycopg2-binary==2.9.11

# Auth
python-jose==3.5.0
passlib==1.7.4
argon2-cffi==25.1.0
python-multipart==0.0.20

# Config
python-dotenv==1.1.1
pydantic==2.11.7
email-validator==2.2.0     # pydantic EmailStr support

# HTTP
requests==2.32.4

# AI — Phase 2 orchestrator (see ai/llm_client.py)
# Groq is the provider actually configured today (GROQ_API_KEY in .env) — its
# API is OpenAI-compatible, so the openai SDK is reused rather than adding a
# second HTTP client dependency.
openai==1.93.0

# Anthropic — drop-in upgrade path, gated behind a real business decision:
# uncomment when client has paid and ANTHROPIC_API_KEY is set.
# anthropic==0.116.0
# headroom-ai==0.28.0   # context compression proxy/lib in front of Claude — see ai/llm_client.py

# Object storage — backend/storage.py's S3 backend (STORAGE_BACKEND=s3).
# Imported lazily there, but listed here so the branch actually works once
# toggled on instead of ImportError-ing at first use.
boto3==1.38.37

# Monitoring
sentry-sdk==2.29.1

# Rate limiting — Phase 2
slowapi==0.1.9

# Scheduling (WhatsApp morning briefing cron)
apscheduler==3.10.4

# WhatsApp / Twilio
twilio==9.6.3

# Web Push (VAPID) — staff notification channel, independent of WhatsApp/Twilio
pywebpush==2.0.3
py-vapid==1.9.0

# Testing
pytest==8.4.1
httpx==0.28.1