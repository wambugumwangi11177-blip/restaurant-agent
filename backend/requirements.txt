# Core
fastapi==0.128.8
uvicorn==0.40.0
gunicorn==23.0.0

# Database
sqlalchemy==2.0.46
alembic==1.18.4
psycopg2-binary==2.9.11

# Auth
python-jose==3.5.0
passlib==1.7.4
argon2-cffi==25.1.0
python-multipart==0.0.22

# Config
python-dotenv==1.2.1
pydantic==2.12.5

# HTTP
requests==2.32.5

# AI — Phase 2 orchestrator (see ai/llm_client.py)
# Groq is the provider actually configured today (GROQ_API_KEY in .env) — its
# API is OpenAI-compatible, so the openai SDK is reused rather than adding a
# second HTTP client dependency.
openai==1.58.1

# Anthropic — drop-in upgrade path, gated behind a real business decision:
# uncomment when client has paid and ANTHROPIC_API_KEY is set.
# anthropic==0.116.0
# headroom-ai==0.28.0   # context compression proxy/lib in front of Claude — see ai/llm_client.py

# Monitoring
sentry-sdk==2.52.0

# Rate limiting — Phase 2
slowapi==0.1.9

# Scheduling (WhatsApp morning briefing cron)
apscheduler==3.10.4

# WhatsApp / Twilio
twilio==9.3.5

# Testing
pytest==8.4.2
httpx==0.28.1
