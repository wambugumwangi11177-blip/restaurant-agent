"use client";

// Reused directly from the Owner dashboard — see roi/page.tsx's comment.
import AiOpsPage from "@/app/dashboard/ai-ops/page";

export default function StaffControllerAiOpsPage() {
    return <AiOpsPage />;
}
