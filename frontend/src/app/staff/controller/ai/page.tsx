"use client";

// Reused directly from the Owner dashboard — see roi/page.tsx's comment.
import AiPage from "@/app/dashboard/ai/page";

export default function StaffControllerAiPage() {
    return <AiPage />;
}
